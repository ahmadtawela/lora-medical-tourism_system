
  # CRM System for Medical Tourism

  This is a code bundle for CRM Syatem for lora clinc

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  